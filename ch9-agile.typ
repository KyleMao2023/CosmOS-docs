// 性能观测：保留可复现实验闭环和三类工具的不同职责。

== 面向证据的优化闭环

CosmOS 的性能优化围绕一个可复现负载形成闭环：先用计数器定位工作量，再用命名耗时探针拆分热点；一次只改变一个因素；相同镜像、配置和负载下重复测量；最后关闭诊断插桩运行正式对照。观测服务于归因，不代替功能测试，也不把带探针构建用于最终性能比较。

#[#set text(0.9em)
#figure(
  table(
    columns: (1.2fr, 2fr, 2.2fr),
    [工具], [观测内容], [用途],
    [/proc/io_perf], [缓存命中、缺页和块设备事件计数], [解释文件/内存读路径变化],
    [perf_probe], [指定代码段的调用次数与耗时], [定位锁、查找、复制等局部热点],
    [/proc/syscalls_count], [系统调用调用次数与内核侧累计耗时], [按真实负载排列优化优先级],
  ),
  caption: [性能观测工具与职责],
)
]

关闭 perf_probe 时，调用点保留原代码块而不读时钟、不维护统计。启用诊断构建后，工具以较低开销收集数据；优化是否成立仍要在无探针构建中用功能回归和同一基准确认。计数器用于性能画像，不能替代安全审计记录。
