// 任务与调度：突出策略组合及 SMP 状态不变量。

== 多 hart 执行模型

每个 hart 由一个 Processor 维护当前任务、本地运行队列和调度状态。任务控制块承载进程归属、内核栈、用户上下文及策略私有状态；运行队列负责入队、选取、抢占和负载迁移。计时器与 IPI 主要提出重新调度请求，实际上下文切换发生在受控的调度安全点，避免在任意中断路径中切换任务。

#figure(image("assets/scheduler-architecture-rr-fifo-cfs-eevdf.svg", width: 100%), caption: [CosmOS 多策略调度结构])

调度控制流不会让任务彼此直接跳转：任务阻塞、让出或退出后，先切回本 hart 的 idle 调度上下文，再由调度循环选择下一项工作。退出任务还由 pending_task_release 暂存，直到切离其内核栈后才释放，避免在仍执行于该栈时回收栈页。

运行队列把实时与公平类分开管理。实时优先级为 1–99，每个 hart 用 100 个优先级桶和最高非空优先级缓存快速选取；公平类以 BTreeMap 保存可运行实体。CFS 的排序键为 (vruntime, task_ptr)，EEVDF 的排序键为 (virtual_deadline, task_ptr)，任务指针用于让相同调度值保持稳定次序。

== 实时与公平调度

运行队列先处理实时任务，再选择公平类任务。实时类提供 FIFO 与 RR：FIFO 由优先级和显式阻塞/让出决定运行顺序，RR 额外使用时间片轮转；普通任务进入公平类，在公平类内部按配置选择 CFS 或 EEVDF。

#figure(
  table(
    columns: (1.0fr, 2.0fr, 2.5fr),
    [策略], [选取依据], [设计要点],
    [SCHED_FIFO], [实时优先级与队列顺序], [适用于显式控制运行次序的实时任务],
    [SCHED_RR], [实时优先级与时间片], [同优先级任务轮转运行],
    [CFS], [虚拟运行时间 vruntime], [优先运行累计服务较少的实体],
    [EEVDF], [资格 eligibility 与虚拟截止时间], [先筛选未超额服务实体，再按虚拟截止时间选择],
  ),
  caption: [调度策略的选择依据],
)

EEVDF 是公平类的一种实现方式，而不是独立的实时调度类；SCHED_OTHER 仍映射到公平类，SCHED_DEADLINE 也不作为已实现的独立调度类。EEVDF 在 vruntime 所表达的公平性之外引入 lag/资格判断与虚拟截止时间，使短期服务请求能获得更清晰的延迟次序。

CFS 将实际运行时间按 nice 权重折算为虚拟运行时间：nice 越高、权重越大，同样的 CPU 服务使 vruntime 增长越慢。当前参数以 24 ms 作为目标延迟窗口、3 ms 作为最小粒度、1 ms 作为唤醒抢占余量；长期睡眠后重新唤醒的任务会被放置在 min_vruntime 附近，而不是带着过期的小值获得无限补偿。

EEVDF 继续使用相同的权重与 vruntime 账本，但为每次公平服务请求计算虚拟截止时间：virtual_deadline = vruntime + request_length × NICE_0_LOAD / weight。调度器先以加权平均虚拟运行时间筛出 eligible 实体，再从中选截止时间最早者；默认请求长度为 3 ms。运行任务暂时不在 fair tree 中，队列保留加权 vruntime 汇总，在比较时再把当前任务纳入平均值。这样既避免每次选择都扫描完整任务集合，也保持 nice 权重表达的长期公平性。

#figure(
  table(
    columns: (1.2fr, 1.8fr, 2.1fr),
    [触发点], [状态变化], [调度器响应],
    [RR 时间片耗尽], [remaining_slice 归零], [同优先级有竞争者时轮转；否则重置时间片继续运行],
    [fair 任务唤醒], [新任务进入公平树], [比较 vruntime 或 eligible deadline 后请求安全点抢占],
    [跨 hart 唤醒], [目标任务进入远端运行队列], [发送 IPI，仅请求重调度，不在中断内直接切换],
    [主动让出], [任务重新排入队列], [普通任务施加公平让出惩罚，实时任务遵循本类队列语义],
  ),
  caption: [常见调度事件与重新调度路径],
)

== SMP 正确性与唤醒

任务从运行态切出时，on_cpu 必须保持为真，直到汇编切换保存完上下文；远端唤醒者在此期间不能把任务提前放入另一 hart 的运行队列。切换完成后以 Release 发布状态，唤醒方用 Acquire 观察并在入队前再次核验 on_cpu/on_rq。对本地中断可能插入的阻塞窗口，调度路径用 LocalIrqSave 将状态变更和 schedule 过渡原子化，避免同 hart 中断在半切换状态自旋。

这组状态不变量把任务状态、运行队列和上下文保存顺序连接起来，是多核调度正确性的核心。策略细节可以替换，但切换安全点、远端重验证和中断屏蔽边界必须保持一致。

亲和性掩码限制任务可运行的 hart 集合。入队优先使用仍有效的 preferred hart；公平任务再按队列负载和运行实体数选择较空闲 hart。负载窃取仅针对亲和性允许的公平任务，不跨 hart 抢取实时任务，以免负载均衡破坏实时优先级和唤醒延迟。所有策略最终汇入相同的 Processor.current、运行队列状态和 trap 尾部重调度入口。
