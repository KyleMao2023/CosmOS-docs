// This file should be included in the `main.typ` file.

== 系统定位与设计主线

CosmOS 是一个面向真实用户程序运行环境的实验型操作系统内核。它起步于rCore，并完成了深度重构。相比只覆盖最小系统调用集合的教学内核，它更强调三个方向：其一，尽可能提供接近 Linux 的进程、文件、信号、网络与设备语义；其二，在 SMP 环境下保证调度、阻塞唤醒、页表修改与资源回收的并发正确性；其三，通过硬件抽象层将内核主体与具体指令集、平台设备解耦，使同一套核心逻辑能够运行在 RISC-V 64 与 LoongArch 64 两种架构上。

本项目的实现思路不是为每个系统调用分别堆叠一条独立路径，而是先抽出若干稳定的内核基础设施，再由它们共同承载上层语义。任务调度负责 CPU 时间和运行状态，进程管理负责资源归属与生命周期，内存管理负责地址空间和页对象，文件子系统用统一文件对象承载普通文件、设备、管道、终端和套接字，等待队列与信号机制负责把异步事件重新接回阻塞路径，HAL 则把陷阱、中断、分页和时钟等硬件事实收敛到统一接口。后续各章虽然按模块展开，但这些模块在实际执行路径中始终互相配合。

#figure(image("assets/ch1-summary.pdf", width: 96%), caption: [CosmOS系统总览图])

// 图片绘制说明：如果需要在第一章加入系统总览图，建议绘制 `assets/ch1_system_overview.svg`。
// 图的目标：用一张简洁的四层架构图展示 CosmOS 的整体局面，不展开细节 API。
// 建议布局：
//   1. 顶层：用户程序与 Linux 兼容运行环境。可标注“进程 / 文件描述符 / 信号 / 套接字”四个代表性名词。
//   2. 第二层：系统服务层。分成“文件子系统”“信号与等待队列”“网络栈”“虚拟文件与设备”四块。
//   3. 第三层：内核核心层。分成“调度与进程”“内存管理”“同步与资源回收”三块。
//   4. 底层：HAL 与平台层。左右分支标注 “RISC-V 64” 与 “LoongArch 64”，中间写 “Trap / IRQ / Paging / Timer”。
// 箭头关系：
//   用户程序向下进入系统服务层；系统服务层依赖内核核心层；内核核心层通过 HAL 接触硬件。
//   可以额外画一条横向强调“等待队列 / poll / 信号”贯穿文件、进程和网络。
// 风格要求：
//   白底、浅色分层、少量箭头；不要画成调用链，也不要列出具体系统调用名。
//   重点突出“统一对象模型”“阻塞—唤醒基础设施”“跨架构 HAL”三条主线。

== 子系统分工

CosmOS 可以按“硬件平台、内核核心、系统服务、用户接口”四层来理解。最底层是硬件平台与 HAL：RISC-V 64 和 LoongArch 64 在陷阱、中断、分页、定时器与多核启动上的差异都被封装在 `hal`、`arch` 与 `platform` 相关模块中。上层内核不直接操作这些硬件细节，而是通过一组稳定的架构抽象获得统一能力。

内核核心层由调度、进程、内存与同步机制构成。调度器维护多 hart 上的运行队列与任务切换，进程管理组织进程组、线程组、文件描述符表和退出回收；内存管理则负责物理页、页表、地址空间、缺页处理、写时复制与 TLB 一致性。这一层决定了系统最基本的不变式：一个任务何时可运行、一个地址何时可访问、一张物理页何时可以被释放。

系统服务层建立在核心层之上，负责把内核能力组织成用户可见的对象。文件子系统以 VFS 和统一文件对象为中心，向下连接多种磁盘后端与块设备，向上承接文件描述符，并同时容纳设备文件、procfs、tmpfs、pipe、tty 等特殊对象。信号、等待队列与 polling 子系统提供统一的阻塞—唤醒基础设施，使文件、进程、同步原语和套接字都能以一致的方式处理超时、信号中断与并发唤醒。网络栈在这一层接入 smoltcp 与 VirtIO-net 驱动，对外呈现 socket 抽象，并把网络事件纳入同一套 poll 机制。

#figure(
  table(
    columns: (1.0fr, 2.1fr, 2.2fr),
    [层次], [主要职责], [后续章节],
    [任务与进程], [CPU 时间分配、线程 / 进程生命周期、资源继承与退出回收], [第 2、3 章],
    [内存管理], [物理页、页表、地址空间、缺页、写时复制与 TLB 一致性], [第 4 章],
    [文件子系统], [统一文件对象、多后端文件系统、缓存体系与虚拟文件], [第 5 章],
    [事件机制], [信号、等待队列、futex、polling 与竞态防护], [第 6 章],
    [网络栈], [smoltcp 驱动模型、VirtIO-net、TCP/UDP/Unix socket、poll 集成], [第 7 章],
    [硬件抽象层], [RISC-V / LoongArch 的陷阱、分页、中断、定时器与平台适配], [第 8 章],
  ),
  caption: [CosmOS 各子系统的职责分工],
)

这种分工并不意味着各子系统彼此独立。相反，CosmOS 中许多关键路径都横跨多个章节。例如，一次文件读取会同时经过文件描述符、统一文件对象、缓存、用户缓冲区翻译与等待队列；如果数据尚未就绪，任务还可能被信号或 poll 事件重新唤醒。一次网络接收也类似：套接字层推动协议栈，网卡中断只留下需要继续推进的标记，真正的数据到达再通过等待队列与 poll 注册表通知用户任务。由此可见，本文档后续章节虽然按模块展开，但读者应把它们理解为同一套内核运行路径上的不同侧面。

== 开发分工与协作

本项目由三位成员协作完成。开发过程大体可以分为三个阶段：基于 rCore 的内核功能扩展阶段、各模块专项分工开发阶段，以及最终的测试调优阶段。三个阶段的工作重心不同：前期追求快速补齐基础功能，中期强调模块边界和语义一致性，后期则集中处理性能瓶颈、并发竞态和跨架构差异。

在基于 rCore 的内核功能扩展阶段，三位成员以 Basic Test 为依据，并行扩展基础系统调用与内核功能。这一阶段的目标是尽快形成可运行的系统骨架，因此实现上更重视语义覆盖和测试通过率，性能开销与内部结构则留到后续阶段逐步整理。为了降低并行开发带来的回归风险，我们在这一阶段引入 GitHub Workflow，对每次 PR 执行自动化测试，保证主分支始终维持基本可用状态。

在基本覆盖简单测例之后，项目进入专项分工阶段。此时系统调用数量、用户程序复杂度和内核内部状态都显著增加，继续按零散任务推进会导致接口语义不稳定。因此我们将任务划分为若干长期方向：调度与进程、信号与网络、文件系统与内存管理、HAL 与平台适配、测试与自动化工具等。每个方向由相对固定的成员主导，但涉及跨模块语义时仍通过共同调试和代码评审保持一致。

最后，在通过大部分测例后，工作重心转向系统测试与调优。我们补充了面向测试集的快速迭代流程和性能观察手段，用于定位文件 I/O、内存回收、阻塞唤醒、网络 polling 等路径上的热点问题。这个阶段的工作很难再严格按模块切开：一个性能问题往往同时涉及系统调用入口、缓存、调度和等待队列；一个并发 bug 也可能横跨中断、锁序和任务状态切换。因此三位成员更多以协作方式推进，围绕具体测试失败或性能瓶颈共同定位、修复和回归验证。

#figure(image("assets/Contributors.png", width: 96%), caption: [GitHub仓库Contributors页面]) <contributors>

#[
#set text(0.95em)
#figure(
  table(
    columns: (1fr, 2.2fr, 1.8fr),
    [成员], [主要负责方向], [协作内容],
    [李其泽], [调度、进程管理], [敏捷开发系统搭建],
    [毛顿开], [信号系统、网络栈、硬件抽象层], [针对性测试脚本撰写],
    [吴立文], [文件子系统、内存管理], [Workflow搭建],
  ),
  caption: [开发者分工],
)
]

@contributors 可以作为开发过程的一个缩影：人均百次以上的commit记录，每次提交都经过自动化测试验证，确保主分支始终可用。三位成员在各自负责的方向上独立、扎实推进，同时在跨模块语义、性能调优和并发问题上保持密切协作。

在项目上，我们使用清晰的目录结构组织代码，自主编写的非注释代码量达到55,000行。各个模块划分的代码量如下图所示。

#figure(image("assets/kernel_code_treemap.svg"), caption: [CosmOS 内核代码量分布图])