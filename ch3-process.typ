// 进程管理：围绕隔离边界、创建、映像替换和资源回收组织。

== 进程与资源边界

进程是资源归属单位，线程是调度与执行单位。进程控制块关联地址空间、文件描述符表、信号状态和线程集合；PID 标识进程，TID 标识进程内线程。线程可共享进程资源，独立进程则使用各自的用户地址空间。用户态指针不作为内核指针直接解引用，而是经地址空间映射与权限检查后访问。

#figure(image("assets/process2.drawio.pdf", width: 95%), caption: [进程创建、执行与回收路径])

启动时，内核构造初始进程并装载配置的 init 映像；进程建立 PID、主线程、用户栈和 trap context 后才进入可运行状态。PCB 负责进程级资源，TaskControlBlock 负责单线程执行上下文；PID 与 TID 分离，允许线程共享进程资源而仍独立调度和退出。

== 创建与映像替换

clone 根据标志组合决定资源继承关系。普通 fork-like 创建为子进程建立独立地址空间，私有页通过 COW 延迟复制；指定 CLONE_VM 的线程或共享 VM 路径按语义复用地址空间。决赛优化进一步把连续页的父子页表更新合并为批次，避免每页重复遍历页表。glibc posix_spawn 使用的 vfork 借用路径见决赛负载优化章节。

clone 标志决定哪些资源共享，未共享的资源由子进程持有自己的引用或副本：

#figure(
  table(
    columns: (1.4fr, 2.1fr, 2.6fr),
    [标志/路径], [共享关系], [影响],
    [CLONE_VM], [地址空间], [线程共享映射；fork-like 默认走独立地址空间和 COW],
    [CLONE_FILES], [文件描述符表], [共享表项变更可见；不设置时子进程获得独立表],
    [CLONE_SIGHAND], [信号处置表], [线程组内共享 handler 配置],
    [CLONE_THREAD], [线程组身份], [线程使用独立 TID，共享进程级资源],
    [CLONE_CHILD_CLEARTID], [线程退出通知], [退出时清除用户 TID 并唤醒等待者],
  ),
  caption: [进程与线程创建中的资源共享边界],
)

execve 在保留进程身份的同时替换用户映像：读取 ELF 头和程序头，为 PT_LOAD 段建立 VMA，按需缺页再装入实际访问的页。这样的懒装载缩短启动路径，也使未访问的文件页无需提前读入。

== 退出与等待

线程退出先停止执行并释放线程级资源；进程退出关闭文件、拆除地址空间并通知父进程。退出状态暂存为 zombie，供 wait/waitpid 读取后完成最终回收。资源释放与锁持有范围分离，避免在进程全局锁内执行耗时 I/O 或对象析构。

进程隔离由独立页表、VMA 权限和受控的用户缓冲区访问共同提供；COW 只在明确的共享语义或写入时复用页面，不混淆两个进程的私有写入。创建、映像替换和回收的页级细节见内存管理章节。

#figure(image("assets/process_exit_reclaim_inkscape.svg", width: 90%), caption: [进程退出与资源回收顺序])

wait/waitpid 负责读取退出状态并清理 zombie；线程同步接口则回收线程任务和 TID 项。回收时先从进程表、线程表和等待对象中解除可见关系，再在锁外释放地址空间、内核栈与文件引用。这样，PID/TID 可见性、父子通知和 Rust 所有权析构各自有明确边界，不要求某一个全局锁覆盖所有资源释放。
