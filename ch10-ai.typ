// This file should be included in the `main.typ` file.

== 使用范围

在 CosmOS 的开发和文档整理过程中，我们使用了 AI 工具辅助完成部分非决策性、可审查的工作。AI 工具主要承担“提高效率”和“提供草稿”的角色，而不是替代开发者进行最终设计判断。所有进入仓库的代码、文档和实验结论，仍由项目成员阅读、修改、验证和确认。

AI 工具的使用主要集中在四类场景。第一类是文档写作辅助，包括根据已有实现和章节内容生成段落初稿、润色表达、整理章节结构，以及把零散的设计记录改写成较连贯的说明文字。第二类是代码阅读辅助，用于快速定位相关模块、总结调用关系、解释某段实现的意图。第三类是调试与测试辅助，用于根据错误现象提出可能的排查方向、生成临时测试脚本或整理复现实验步骤。第四类是图表说明辅助，用于把系统结构、数据通路或协作流程转化为绘图需求，再由成员或其他绘图工具绘制成最终图像。

== 使用原则

我们对 AI 工具的使用遵循三个原则。

首先，AI 生成内容必须可追溯到项目已有事实。文档中的系统结构、性能数据、接口语义和实现细节，不能仅凭 AI 的一般经验直接写入；需要来自仓库代码、测试结果、提交历史或成员确认。对于不确定内容，文档中应当避免写成确定结论，或者保留为待补充项。

其次，AI 不负责最终技术决策。涉及内核语义、并发正确性、锁序、内存生命周期、跨架构 ABI、性能优化取舍等问题时，AI 可以帮助整理可能方案，但最终采用哪种设计、是否合入代码、如何解释实验结果，均由开发者根据代码和测试决定。

再次，AI 生成内容需要人工审查。尤其是代码修改建议和技术描述，需要检查是否符合项目约束，例如新增代码注释风格、不得使用 `fmt`、测试点位置、Typst 文档格式，以及是否与已有章节说法一致。对于文档，AI 生成的段落还需要进一步统一语气、删去过度枚举的 API 清单，并避免写入未经验证的性能数字。

== 具体用途

#figure(
  table(
    columns: (1.2fr, 2.2fr, 1.8fr),
    [用途], [具体内容], [人工处理],
    [文档草稿], [根据已有章节整理概述、总结、展望和模块说明], [成员审阅、删改、补充事实],
    [代码理解], [辅助梳理调用路径、模块边界和关键数据结构], [以源码为准重新核对],
    [调试建议], [根据测试失败或性能现象给出排查方向], [实际运行测试并验证结论],
    [图示需求], [把架构图、流程图], [成员调整、校对，并生成最终图片],
  ),
  caption: [AI 工具使用范围],
)

在本文档中，AI 工具主要用于生成和调整部分章节的文字草稿，例如概述、总结与展望、AI 工具使用情况等偏总结性的内容；对于内存管理、文件系统、信号、网络栈、HAL 等技术章节，AI 只作为整理表达和检查连贯性的辅助，具体技术内容仍以源码、实验记录和成员实现为依据。

== 局限与防护

AI 工具有两个明显局限。第一，它可能把常见操作系统设计经验错误套用到本项目中，生成与 CosmOS 实际实现不一致的描述。第二，它可能在缺少上下文时补全不存在的细节，例如不存在的接口、未经测量的性能结论或没有实现的特性。因此，我们在使用时尽量让 AI 先阅读本仓库中的相关文档和代码，再基于已有内容改写，而不是直接凭空生成。

针对这些局限，我们采用了几类防护措施：涉及代码的修改必须经过编译或测试；涉及性能的结论必须有实验数据或计数器支撑；涉及文档结构的改动要和 `main.typ` 的章节顺序保持一致；涉及图示的内容优先写成绘图说明，等图片真正完成后再引用资源文件。这样可以让 AI 的输出停留在“可编辑草稿”和“辅助说明”层面，避免它直接成为未经验证的最终事实。

总体上，AI 工具在本项目中的作用是提高信息整理、文档撰写和问题排查的效率。它帮助我们更快地把已有实现转化为可读说明，也能在调试时提供备选思路；但 CosmOS 的设计、实现、测试和最终结论仍由开发者负责。
