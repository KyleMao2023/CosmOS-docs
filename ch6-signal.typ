// 信号与事件等待：以共享唤醒原语、代计数器和并发不变量为主线。

== 统一阻塞—唤醒机制

文件读取、futex、信号等待、套接字和多路 I/O 都需要在“条件尚未满足”时阻塞，并在状态变化后准确唤醒。CosmOS 以 WaitQueue、按键等待队列和可类型擦除的等待句柄复用这条路径。等待过程先登记、再复查条件、最后睡眠；唤醒方先更新就绪状态，再选择匹配等待者。复查步骤封闭了“检查条件与进入睡眠之间”的丢失唤醒窗口。

#figure(image("assets/wait_queue.svg", width: 84%), caption: [等待队列与唤醒句柄])

等待句柄带有槽位代计数。超时回调或设备通知若迟于注销到达，会因代号不匹配而被忽略，不会唤醒后来复用同一槽位的任务。多个子系统共享这一协议，但各自保留需要的唤醒条件和返回语义。

== 信号与多路事件

信号投递设置进程级或线程级挂起状态，并经等待句柄唤醒相应阻塞任务。可中断的系统调用在复查后返回 EINTR；设置 SA_RESTART 且符合重启条件时，信号返回路径恢复原参数并重试系统调用。体系结构相关的信号帧布局由 SignalAbi 承接，投递与信号掩码逻辑保持共用。

ppoll 以固定容量的二维位图集中登记 fd/source 与等待 key 的关系。事件源状态变化后，内核收集匹配 key、释放注册表锁，再执行唤醒；超时和注销路径核验代号。容量耗尽时退回短周期睡眠并重新扫描，避免把内部表容量错误暴露给用户态。

#figure(image("assets/poll_bitmap.svg", width: 88%), caption: [二维位图等待注册表])

=== 信号帧与系统调用恢复

信号状态区分进程级挂起和线程级挂起，屏蔽字决定当前线程何时可处理信号；SigInfo 保存发送者与原因等 ABI 字段。信号抵达时先标记挂起状态，再沿等待句柄唤醒可能阻塞的线程。真正投递发生在返回用户态前：内核按 SignalAbi 构造用户栈帧，记录寄存器、信号掩码和返回地址，再跳转到用户 handler。

#figure(image("assets/signal_add.pdf", width: 100%), caption: [信号投递与用户态处理路径])

rt_sigreturn 从用户栈帧恢复寄存器和屏蔽字。对于 SA_RESTART 且语义允许重启的系统调用，trap context 保存原始第一参数与系统调用状态；信号返回时把 PC 回退到系统调用指令并恢复原参数，使用户态透明重试。架构只提供指令长度和 ucontext 布局，重启判定与投递策略由通用逻辑完成。sigtimedwait 则登记目标信号集合，醒来后同步取走匹配信号，而不走普通 handler 路径。

epoll 面向持久兴趣集与就绪队列，沿用统一的 source_id 通知接口；边沿触发项通过原子就绪状态避免同一边沿重复入队。epoll_pwait/pwait2 与信号掩码结合，eventfd 和 signalfd 则将计数与信号转成普通可等待文件。以上机制已用于构建工具链事件循环。

== SMP 竞态与保护不变量

唤醒与阻塞的状态转换跨越任务锁、运行队列和上下文切换，不能只依赖“持有一把锁”。远端唤醒遇到仍在 CPU 上的任务时，须等上下文完成保存并重新核验 on_cpu/on_rq；本地中断屏蔽则防止同 hart 中断在半阻塞窗口重入并自旋。等待注册表使用代计数识别迟到通知，锁内只更新状态和收集 key，锁外再唤醒，避免在临界区内触发调度。

#figure(image("assets/race_condition_1.svg", width: 90%), caption: [SMP 下唤醒与阻塞之间的状态竞争])

同 hart 的竞态有另一条死锁路径：若本地硬中断在任务已从 Processor.current 摘除、但上下文尚未切走时抢入，唤醒处理可能等待本 hart 自己完成切换，形成自旋闭环。LocalIrqSave 将摘除 current 到 schedule 的窗口关在本地中断屏蔽内；远端 hart 则通过 on_cpu 的 Acquire/Release 发布和重验证等待安全时点。

#figure(image("assets/race_condition_2.svg", width: 90%), caption: [本地中断观察到半阻塞状态时的自等待竞态])

这套设计把安全性落在可检查的不变量上：先发布条件，再唤醒；先完成上下文切换，再允许远端重新入队；复用槽位时必须更新代号；唤醒动作不在注册表锁内执行。
