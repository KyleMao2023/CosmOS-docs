// This file should be included in the `main.typ` file.

== 工作总结

CosmOS 的开发目标，是在一个可教学、可实验的内核框架上，尽可能接近真实 Linux 用户程序所依赖的运行环境。围绕这一目标，项目最终形成了从底层硬件抽象到上层系统服务的完整链条：HAL 将 RISC-V 64 与 LoongArch 64 的体系结构差异压入统一接口；调度、进程与内存管理提供任务执行、资源归属和地址空间隔离；文件、信号、等待队列、polling 与网络栈则共同支撑用户态程序的 I/O、异步事件和进程间交互。

从结构上看，CosmOS 的核心收获不只是“实现了若干功能”，而是逐步建立了一套相对稳定的内核组织方式。文件描述符背后的统一文件对象，使普通文件、设备、管道、终端与套接字可以共享系统调用入口和 poll 模型；等待队列、信号与注册表机制，使阻塞系统调用、futex、poll 和套接字超时能够复用同一种防丢失唤醒思路；内存管理中的 VMA、页表、page cache、COW 与 TLB shootdown，则把用户态可见的地址空间语义落到明确的页生命周期与跨 hart 同步协议上。不同章节讨论的是不同模块，但它们共同服务于同一个目标：让用户程序看到一致、可预期、接近 Linux 的系统行为。

从工程过程看，项目经历了从功能补齐到专项分工，再到性能调优和稳定性修复的转变。早期更关注测试覆盖和系统调用语义，中期开始划分调度、内存、文件、信号、网络、HAL 等长期方向，后期则大量时间花在性能归因、并发竞态、跨架构差异和根文件系统兼容性上。第九章介绍的 `/proc/io_perf` 与 `perf_probe` 正是在这一阶段形成的工具：它们让优化不再停留在直觉层面，而是可以用计数器、耗时探针和可复现实验解释每一次改动的收益与代价。

总体而言，CosmOS 已经从一个基础教学内核扩展为一个具有多子系统协同能力的实验平台。它能够运行更复杂的用户程序，能够在 SMP 环境下处理大量阻塞与唤醒路径，能够在两个架构上复用主体代码，也能够通过 procfs 与性能探针观察内部行为。这些能力为后续继续完善 Linux 兼容性、提升性能和扩展硬件平台打下了基础。

== 主要不足

CosmOS 仍然存在不少限制。它们大致可以归为四类：兼容性覆盖、分层带来的性能代价、多核并发风险，以及性能评估体系尚不完整。下表总结了这些不足的表现、影响和当前状态。

#[
#set text(0.92em)
#figure(
  table(
    columns: (1.1fr, 2.0fr, 2.0fr, 1.3fr),
    [不足方向], [具体表现], [主要影响], [当前状态],
    [Linux 兼容性], [部分边角语义、错误码、特殊文件行为、socket 选项与 ioctl 尚未完整覆盖], [真实用户程序可能在测试集之外遇到兼容性差异], [已覆盖常用路径，仍需持续补齐],
    [缓存与 I/O 分层], [page cache 与 block cache 存在文件数据双重缓存；系统调用入口仍有固定开销], [内存占用增加，小块 I/O 与随机访问仍可能受限], [已有分级缓存和快路径，但尚未统一缓存体系],
    [网络栈扩展性], [基于 smoltcp 的协作式 polling 模型依赖全局协议栈锁，收发路径仍有逐帧拷贝], [多连接和高吞吐场景下可能出现锁竞争与拷贝瓶颈], [功能完整度优先，性能仍有优化空间],
    [并发正确性], [任务状态、等待队列、TLB shootdown、页回收和中断尾部存在复杂交错], [极端时序下仍可能出现长尾延迟、误唤醒或资源回收竞态], [已用锁序、代计数器和三段式阻塞解决一批问题],
    [性能评估体系], [优化多围绕测试集和具体瓶颈推进，缺少长期统一 benchmark 集合], [不同优化之间难以在吞吐、延迟、内存占用和公平性上系统比较], [已有 io_perf 与 perf_probe，仍需体系化沉淀],
  ),
  caption: [当前主要不足及影响],
)
]

这些不足并不意味着现有设计失效，而是反映了实验内核向更真实负载推进时必然遇到的边界。当前实现优先保证结构清晰、语义可解释和测试可通过；后续工作需要在保持这些性质的前提下，逐步降低性能开销并扩大兼容范围。

== 后续展望

针对上述不足，后续工作可以按“先补语义、再降成本、最后扩展规模”的思路推进。下表列出了几个较明确的改进方向。

#[
#set text(0.92em)
#figure(
  table(
    columns: (1.2fr, 2.2fr, 1.7fr, 1.2fr),
    [改进方向], [具体措施], [预期收益], [优先级],
    [完善 Linux 兼容性], [补齐 socket 选项、ioctl、procfs/sysfs 节点、权限检查、挂载语义和更多文件系统边界情况], [提升真实用户程序可运行范围，减少测试外差异], [高],
    [统一缓存与 I/O 路径], [研究 page cache 与 block cache 的统一；降低 fd 查找、用户缓冲翻译和小块 I/O 的固定成本], [减少重复缓存，提高文件 I/O 吞吐并降低内存占用], [高],
    [优化网络栈], [降低全局协议栈锁竞争，引入更好的收发缓冲复用，探索批处理或更少拷贝路径], [提升多连接吞吐和网络延迟稳定性], [中],
    [提升多核扩展性], [拆分热点全局锁，引入 per-hart 缓存、分片队列和更细粒度唤醒路径], [降低 SMP 下锁竞争和跨 hart 同步成本], [中],
    [强化并发验证], [增加压力测试、长时间随机负载、竞态复现脚本和关键不变式检查], [更早发现极端时序下的误唤醒、死锁和资源生命周期问题], [高],
    [沉淀性能评估体系], [把 io_perf、perf_probe、自动化测试和 benchmark 脚本整合为持续回归流程], [让性能优化可比较、可复现、可回退], [中],
    [扩展平台支持], [继续完善 HAL 与平台层，支持更多设备模型、中断控制器和启动环境], [验证抽象边界，提升系统可移植性], [低到中],
  ),
  caption: [后续改进方向与预期收益],
)
]

如果按实施路径划分，可以进一步分成近期、中期和长期三类目标。

#[
#set text(0.92em)
#figure(
  table(
    columns: (0.8fr, 2.6fr, 2.0fr),
    [阶段], [重点任务], [验收方式],
    [近期], [围绕测试集和常用用户程序补齐兼容性缺口，清理临时兼容分支，补充回归测试], [更多真实程序稳定运行，错误码与边界行为更接近 Linux],
    [中期], [重构缓存与 I/O 热路径，降低全局锁竞争，完善性能观测和 benchmark 流程], [同一负载下吞吐、延迟和内存占用有可复现改善],
    [长期], [扩展平台与设备支持，系统化验证并发不变式，形成更完整的内核实验平台], [跨架构构建和测试常态化，复杂负载下保持稳定],
  ),
  caption: [后续工作的阶段性安排],
)
]

CosmOS 当前仍是一个不断演进的实验内核，但它已经形成了比较清晰的工程方向：用分层结构管理复杂性，用共享机制减少重复实现，用观测工具约束性能优化，用跨架构抽象验证设计边界。后续工作的重点，不再只是继续“加功能”，而是让这些功能在更复杂的负载、更强的并发和更接近真实 Linux 环境的场景下保持一致、稳定和可解释。
