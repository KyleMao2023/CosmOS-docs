// 内存管理：保留地址空间隔离、按需装页、COW 与跨 hart 一致性。

== 管理对象与架构边界

内存管理连接物理页分配、页表映射和进程可见的 VMA 语义。物理页分配器为页表、内核对象和用户页提供页框；PageTable 维护硬件映射；VMA 描述一段地址范围的读写执行权限、匿名/文件后备关系及共享方式。将 VMA 语义与页表映射分开，使权限检查、缺页处理和映射回收围绕同一地址空间模型完成。

#figure(image("assets/mm_overview.pdf", width: 82%), caption: [内存管理对象与数据通路])

RISC-V Sv39 与 LoongArch 页表格式、地址规范化和 TLB 指令不同，统一页表遍历通过 HAL 的 PagingArch 接口调用相应实现。内核地址映射、direct map 与用户低地址空间分区也由架构布局约束；陷入路径避免在不需要时重建映射，决赛阶段的高半区共享和 ASID 优化见后文。

== 物理页框与 Rust 所有权

物理页框由启动信息驱动初始化。内核读取 bootinfo 中可用物理区间，排除内核镜像和保留区域后交给 buddy allocator 管理；每个受管范围记录在页号区间表中，释放时再次检查范围，避免把设备保留页或未管理页放回空闲链表。

buddy 按 order 拆分和合并连续页块。释放页时用页号异或当前块大小定位 buddy，只有 buddy 同阶且确实空闲才合并。决赛阶段用双向空闲链和位图索引缩短合并查找，并为每个 hart 增加 32 页本地缓存，以 8 页为批次与全局 buddy 回填/上缴，降低 rustc 高缺页负载下的全局分配锁竞争。

普通页框由 FrameTracker 表达所有权：创建时清零，最后一个 Rust 引用离开时自动归还分配器。页表页、匿名页和缓存页因而共享统一的 RAII 生命周期；需要连续物理区间的对象由连续页跟踪器整体持有。内核页表中的永久子树则由 KERNEL_SPACE 生命周期持有，不交给临时用户 PageTable 释放。

== 用户地址空间与访问控制

每个进程的 MemorySet 描述自己的用户映射。VMA 将区间、权限与后备对象关联起来；映射和缺页路径据此校验访问类型，不能写入只读页，也不能从不可执行区域取指。系统调用读写用户缓冲区时，经地址空间翻译得到受控页片段，跨页缓冲区按映射逐段处理。

#figure(image("assets/cosmos_process_vm_layout_rv_la.svg", width: 100%), caption: [用户地址空间布局]) <vm_layout>

brk 与 mmap 更新 VMA 元数据，普通映射不会立刻分配所有物理页。首次访问触发缺页后，内核判断访问是否合法，再按匿名页、文件页、COW 或错误路径处理。ELF 装载同样先登记 PT_LOAD 区间，未访问页延后到缺页时从 Page Cache 取出。

#figure(image("assets/mm_demand_fault_flow.drawio.pdf", width: 88%), caption: [按需装页、文件映射与写时复制路径])

== COW、Page Cache 与多核回收

fork 对私有可写映射建立 COW 关系：父子先共享只读物理页，任一方写入时再分配私有页并复制内容。MAP_SHARED 文件映射通过 Page Cache 共享页并标记脏状态；MAP_PRIVATE 首次写入后转为私有页。页引用、映射计数和页状态共同约束回收，正在 I/O、仍映射或正在回写的页面不会被直接释放。

页表修改可能影响其他 hart 上正在运行的地址空间。内核先发布映射变更，再通过 IPI/shootdown 使对应 TLB 项失效；在远端确认前，把页框和相关标识放入延迟回收集合。ASID 与按范围刷新降低跨地址空间切换成本，详细的 QEMU/RISC-V 测量与高半区共享设计见决赛负载优化章节。

具体回收顺序是：持地址空间锁摘除 PTE 并收集旧页，同时快照该 mm 当前活跃的 hart；释放锁后向这些 hart 请求地址空间失效，远端完成刷新后再释放旧页引用。COW 替换、mprotect、munmap、exec teardown 和文件截断都遵循这一顺序。内核栈也采用延迟回收：虚拟地址或页框只有在全局 shootdown 后才能复用。该协议把“映射已删除”和“所有 CPU 都不再使用旧翻译”分成两个明确阶段。
